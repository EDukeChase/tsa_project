---
title: "Timerseries Analysis Project - Part 1"
author: "E. Duke Chase"
date: today
---

```{r}
#| label: setup
#| include: false

source(here::here("R", "setup.R"))
```

```{r}
# Load data
ili_raw <- read.csv(here("data", "ILINet.csv"), header = TRUE, skip = 1)

# Clean data
ili_clean <- ili_raw |> 
  # Remove rows with missing data or no unique data
  select(
    -REGION.TYPE,           # All are "National"
    -REGION,                # All are "X"
    -X.UNWEIGHTED.ILI,      # Raw, not weighted by state size
    -contains("AGE")        # Two of the demographic breakdowns are empty
    ) |> 
  rename(
    year = YEAR,
    week = WEEK,
    weighted_ili = X..WEIGHTED.ILI,
    
  )
```

